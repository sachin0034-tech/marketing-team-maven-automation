# Copy this to .env and fill in real values — never commit .env
APP_EMAIL=you@example.com
APP_PASSWORD_HASH=<run: python3 -c "import hashlib; print(hashlib.sha256('yourpassword'.encode()).hexdigest())">